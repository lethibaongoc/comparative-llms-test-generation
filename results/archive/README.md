# Archive

Superseded/dead artifacts kept for record only, not used in analysis.

- `pilot_groq_llama33_70b_FAILED.json` — output of an early automated attempt (`scripts/run_pilot_groq.py`) to call the Groq API directly from the sandbox. All 6 entries have `"status": "INVALID"` (connection error). The team switched to running prompts manually through the Groq Playground UI instead; those real outputs are the 60 `.java` files in `data/generated/llama/`.
